{
  "name": "cxxfilt-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for cxxfilt demangler",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "toucan-js": "^3.1.0",
    "@cloudflare/kv-asset-handler": "^0.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
