{
  "name": "cxxfilt-web",
  "version": "1.0.0",
  "description": "C++ Symbol Demangler Web Frontend",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --emitDeclarationOnly && esbuild src/index.ts src/worker.ts --format=esm --target=es2020 --sourcemap --minify --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "author": "Iris Shi",
  "license": "MIT"
}
